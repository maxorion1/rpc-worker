# Max‑View — Portal‑OS

Small React + Vite skeleton to interact with the Worker endpoints.

Run locally:

1. cd ui/max-view
2. npm install
3. VITE_API_BASE=http://127.0.0.1:8787 npm run dev

Adjust VITE_API_BASE to point at your Worker dev server.
