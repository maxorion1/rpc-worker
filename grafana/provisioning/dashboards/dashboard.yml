apiVersion: 1
providers:
  - name: 'default'
    orgId: 1
    folder: ''
    type: file
    options:
      path: /var/lib/grafana/dashboards
    # Allow UI updates so dashboard edits from Grafana UI are persisted to the Grafana DB
    allowUiUpdates: true
