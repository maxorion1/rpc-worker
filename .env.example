# Example env file for running the docker-compose monitoring stack.
# Copy this to .env and replace the example password with a strong secret.
# Do NOT commit the actual .env file to the repository.

# Grafana administrator credentials (change for production)
GRAFANA_ADMIN_USER=grafana_admin
GRAFANA_ADMIN_PASSWORD=replace_with_a_strong_password_here
